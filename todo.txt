- add tests for future cps cases
  - blocks
  - multiple function args (and, +, *)

- extend cps to make and(flip(), flip()) work
  - blocks
  - multiple function arguments
  - desugar variable declaration to lambda?

- add call/cc to cps transform

- decide whether arrays and method accessors will be allowed in sppl (currently used in head.sppl for sample)

- figure out correct query boundary: what do we do when the computation is done?

- implement forward sampling using continuations (in header.js) [NDG]

- implement enumeration using continuations (in header.js)
